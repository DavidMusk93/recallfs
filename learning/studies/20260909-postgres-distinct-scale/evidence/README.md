# Evidence

Generated by `../demo/scripts/run.sh` on 2026-09-09.

| Artifact | Meaning |
| --- | --- |
| `environment.txt` | Host, Docker VM, PostgreSQL versions, and relevant settings |
| `image-digests.txt` | Exact Docker image digests used by the run |
| `benchmark.csv` | Warm-cache average from one `pgbench` run of seven transactions |
| `correctness.csv` | Both queries compared with each other and an independent expected set |
| `plan-metrics.csv` | Parsed `EXPLAIN ANALYZE` counters for every case |
| `plans/*.json` | Full machine-readable plan for every query and data shape |
| `pgbench.log` | Raw benchmark summaries, including failed transaction counts |
| `source-digests.txt` | Digests of the fetched article and its technical figures |
| `complete.txt` | Publication marker with expected artifact counts |

All 36 benchmark cases returned the configured distinct count. Every recorded
plan had zero heap fetches. PostgreSQL 18 plans also expose `Index Searches`,
which distinguishes one continuous scan from `D+1` B-tree searches.

The Docker VM was intentionally constrained to four vCPUs and 6 GiB RAM. These
numbers are comparative evidence on one host, not a production performance
baseline.
