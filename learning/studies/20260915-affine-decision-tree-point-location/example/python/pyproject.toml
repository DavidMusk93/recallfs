[project]
name = "odt-example-protocol"
version = "1.0.0"
description = "Deterministic data protocol and independent oracle for the ODT example"
requires-python = "==3.13.12"

[tool.uv]
package = false
