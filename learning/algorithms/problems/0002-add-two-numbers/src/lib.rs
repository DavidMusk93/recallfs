//! LeetCode 2. 两数相加
//! https://leetcode.cn/problems/add-two-numbers/
//!
//! 教学闸门：完成 learn.html 理解测（或用户明确跳过）后再填完整解法。

#![allow(dead_code)]

pub struct Solution;

impl Solution {
    /// 在此实现题解。签名需与 leetcode.cn Rust 模板对齐后替换。
    pub fn solve_placeholder() {
        unimplemented!("complete after learn.html gate")
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    #[ignore = "replace with real samples after implementing"]
    fn sample_1() {
        let _ = Solution::solve_placeholder;
    }
}
