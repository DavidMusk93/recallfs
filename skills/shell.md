# Shell

目标：写高性能、稳健、可复现的 shell 命令和脚本，减少无谓 IO、避免脆弱管道、降低误操作风险。

## 1. 核心原则

- 先缩小输入范围，再处理内容。
- 优先使用能直接表达目标的命令，避免用通用文本替换硬凑。
- 避免读取大量文件；必须读取时先采样、过滤或限制行数。
- 所有路径、变量、参数都要加引号。
- 脚本入口使用 `set -euo pipefail`，并显式处理允许失败的命令。
- 参数解析、校验、执行逻辑分离，避免散乱分支。
- 危险操作默认 dry-run，apply 需要显式参数。

## 2. 命令选择

| 目标 | 推荐 | 避免 | 说明 |
| --- | --- | --- | --- |
| 查看 `/proc/<pid>/environ` | `strings` | `tr '\\0' '\\n'` | `strings` 更直接，避免手写替换 |
| 按名称查进程 | `pgrep` / `pidof` | 遍历 `/proc/*` | 专用工具更快、更稳、更少读文件 |
| 查找文件名 | `find` / `fd` | `ls \| grep` | 文件名可能包含空格和换行 |
| 搜索文本 | `rg` | `grep -R` | `rg` 默认更快，且有合理忽略规则 |
| 解析 JSON | `jq` | `grep` / `sed` | 结构化数据用结构化工具 |
| 解析 YAML | `yq` | `awk` 拼接 | 减少格式边界错误 |
| 查看大文件头尾 | `head` / `tail` | `cat` | 避免无意义全量输出 |
| 观察增长日志 | `tail -f` | 循环 `cat` | 降低 IO 和输出噪音 |
| 统计行数 | `wc -l < file` | `cat file \| wc -l` | 少一个进程和管道 |
| 批量处理文件 | `find -print0` + `xargs -0` | `for f in $(find ...)` | 安全处理特殊文件名 |

进程查询示例：

```bash
pgrep -af 'stream_engine'
pidof stream_engine
ps -eo pid,ppid,stat,comm,args | rg 'stream_engine'
```

避免手扫 `/proc`：

```bash
for p in /proc/[0-9]*; do
  cat "$p/cmdline"
done
```

只有拿到明确 pid 后，才读取 `/proc/<pid>` 的细节：

```bash
pid="$(pgrep -n stream_engine)"
[[ -n "$pid" ]] || { echo "process not found" >&2; exit 1; }

strings "/proc/$pid/environ"
cat "/proc/$pid/status"
```

## 3. 性能流程

```text
+----------------------+
| 需要处理数据?         |
+----------+-----------+
           |
           v
+----------------------+
| 先限定目录/文件模式   |
+----------+-----------+
           |
           v
+----------------------+
| 先过滤再读取内容      |
+----------+-----------+
           |
           v
+----------------------+---- yes ---->+----------------------+
| 数据可能很大?         |              | head/tail/rg 限制范围 |
+----------+-----------+              +----------------------+
           | no
           v
+----------------------+
| 使用专用工具处理      |
+----------+-----------+
           |
           v
+----------------------+
| 输出摘要和证据路径    |
+----------------------+
```

## 4. 高性能思考原则

agent 写 shell 前，先判断数据规模、IO 成本和工具语义，再选择命令。

```text
+----------------------+
| 准备写 shell 命令?    |
+----------+-----------+
           |
           v
+----------------------+
| 输入规模有多大?       |
+----------+-----------+
           |
           v
+----------------------+---- large --->+----------------------+
| 能先缩小范围吗?       |              | 先过滤路径/类型/时间 |
+----------+-----------+              +----------------------+
           |
           v
+----------------------+
| 有专用工具吗?         |
+----------+-----------+
           |
           +---- yes ----> 使用专用工具
           |
           +---- no -----> 再组合管道
```

选择顺序：

| 问题 | 优先思考 |
| --- | --- |
| 输入是否很大？ | 先限制路径、后缀、时间、大小 |
| 是否需要全量读取？ | 能采样就采样，能 tail 就不要 cat |
| 是否有专用工具？ | JSON 用 `jq`，进程用 `pgrep`，environ 用 `strings` |
| 输出是否会爆炸？ | 先写日志或限制数量，再输出摘要 |
| 是否存在竞态？ | 动态对象如进程和临时文件要容忍消失 |
| 是否可复现？ | 记录命令、范围、参数和输出路径 |

原则：

- 先问“要处理多少数据”，再问“用什么命令”。
- 先减少输入，再优化管道。
- 优先选择语义正确的工具，而不是更短的命令。
- 对动态系统保持容错，例如进程可能退出、日志可能轮转。
- 输出以证据和摘要为主，不把大段原始内容直接塞给用户。

## 5. 少读大文件

- 先用 `find`、`rg --files`、文件大小、修改时间缩小候选集。
- 对日志优先看 `tail -n 200`，不要直接 `cat`。
- 对未知大文件先用 `wc -c`、`file`、`head` 判断类型。
- 对压缩文件先列目录或采样，不要直接全量解压。
- 对仓库搜索优先加路径和 glob，例如 `rg "pattern" src/ -g '*.go'`。
- 查找进程优先用 `pgrep`、`pidof`、`ps`，不要遍历 `/proc/*` 再读取大量 `cmdline` 或 `status`。
- 输出给用户前做摘要，避免粘贴大量无效日志。

示例：

```bash
find logs -type f -name '*.log' -mtime -1 -size -100M
tail -n 200 logs/app.log
rg -n --max-count 20 'panic|error|failed' logs/
```

## 6. Robust Shell

脚本模板：

```bash
#!/usr/bin/env bash
set -euo pipefail

die() {
  echo "error: $*" >&2
  exit 2
}

main() {
  local input="${1:-}"
  [[ -n "$input" ]] || die "missing input"

  printf 'input=%s\n' "$input"
}

main "$@"
```

关键规则：

- 使用 `"$var"`，不要裸用 `$var`。
- 使用 `[[ ... ]]` 做条件判断。
- 使用 `local` 限定函数变量。
- 使用数组保存参数，避免字符串拼命令。
- 使用 `mktemp` 创建临时文件，不手写固定临时路径。
- 允许失败时显式写出，例如 `cmd || true`，并说明原因。
- 管道中需要读取失败时保留 `pipefail`，不要静默吞错。

## 7. 文件名安全

```bash
find "$root" -type f -name '*.log' -print0 |
  xargs -0 rg -n 'error|panic'
```

避免：

```bash
for f in $(find "$root" -type f); do
  grep error "$f"
done
```

原因：

- 文件名可能包含空格、换行、通配符。
- 命令替换会丢失边界。
- `find -print0` 和 `xargs -0` 能保留文件名边界。

## 8. 参数与交互

- agent 调用的脚本必须支持非交互参数。
- 必须支持 `--help` 或清晰 usage。
- 必填参数缺失时返回稳定错误码，建议 `2`。
- 不要在脚本中临时 `read -p` 等待用户输入。
- 需要确认时使用 `--apply`、`--yes`、`--dry-run` 这类显式参数。

## 9. 日志与输出

- 正常结果写 stdout。
- 错误和诊断写 stderr。
- 大任务输出日志文件，并在最终结果中给出路径。
- 输出要包含关键命令、输入范围、处理数量、失败数量。
- 不要把海量日志直接打到终端；先写文件，再摘要。

## 10. 危险操作

| 操作 | 要求 |
| --- | --- |
| 删除文件 | 默认 dry-run，明确列出目标 |
| 覆盖文件 | 先写临时文件，再原子替换 |
| 批量修改 | 先限制路径和后缀，再执行 |
| 远程同步 | 打印 source 和 destination |
| 后台任务 | 记录 PID、日志路径、停止方式 |

## 11. 检查清单

- 是否避免了不必要的全量读取？
- 是否对大文件使用了 `head`、`tail`、`rg` 或采样？
- 是否选择了专用工具，而不是脆弱文本替换？
- 是否用 `pgrep` / `pidof` 查进程，而不是手扫 `/proc`？
- 是否所有变量和路径都正确引用？
- 是否处理了文件名空格和特殊字符？
- 是否支持非交互运行和 `--help`？
- 是否有稳定退出码和清晰错误信息？
- 是否记录了关键命令、输入范围和输出路径？

## 12. Agent 提醒

- shell 的第一目标是可靠完成任务，不是写最短命令。
- 性能问题通常先来自输入范围过大，再来自命令本身。
- 能不读就不读，能少读就少读，能用索引或过滤就不要全量扫描。
- 用正确工具表达正确语义，例如 environ 用 `strings`，JSON 用 `jq`。
- 查进程先用 `pgrep` / `pidof`，只有拿到明确 pid 后才读取 `/proc/<pid>`。
